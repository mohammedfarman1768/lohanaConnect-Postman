info:
  name: Verify OTP For Registration
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:5000/api/v1/user/auth/verify-otp
  body:
    type: json
    data: |-
      {
        "userId": "69ae5e3c87a1f845828ba550",
        "type": "EMAIL",
        "otp": "751186"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
