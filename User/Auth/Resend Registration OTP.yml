info:
  name: Resend Registration OTP
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:5000/api/v1/user/auth/resend-otp
  body:
    type: json
    data: |-
      {
        "userId": "69ae5e3c87a1f845828ba550", 
        "type": "EMAIL"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
