info:
  name: Request Login OTP
  type: http
  seq: 17

http:
  method: POST
  url: http://localhost:5000/api/v1/user/auth/login/otp/request
  body:
    type: json
    data: |-
      {
       "identifier":"rtsfarman2026@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
